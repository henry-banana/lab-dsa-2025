#pragma once

#include "../../others/header/helpFunctions.h"

#ifndef BUBBLE_SORT_H
#define BUBBLE_SORT_H

template <class T> void bubbleSort(std::vector<T> &arr, size_t &count_comparison);
template <class T> void bubbleSort(std::vector<T> &arr);

#endif // BUBBLE_SORT_H